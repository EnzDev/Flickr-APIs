$(function() {
    $( ".zoom" ).dialog({
		autoOpen: false,
		resizable: false,
		modal: true,
		draggable: false
	});
	        
	$( ".zoom" ).dialog();
	$(".ui-dialog-title").hide();
	$(".ui-dialog-titlebar").removeClass('ui-widget-header');

    
})